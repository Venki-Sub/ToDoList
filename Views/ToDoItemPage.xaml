<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoList.Views.ToDoItemPage"
             xmlns:viewmodel="clr-namespace:ToDoList.ViewModels" 
             xmlns:model="clr-namespace:ToDoList.Models"
             Title="To-Do List">
    <ContentPage.Content>
        <VerticalStackLayout Padding="20">
            <Label Text="To-Do Items" FontSize="24" HorizontalOptions="Center" />
            
            <CollectionView ItemsSource="{Binding ToDoItems}" Margin="0,10,0,0">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="0" Margin="5" Background="#FFFFFF">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10" />
                            </Border.StrokeShape>
                                <StackLayout Padding="10" x:DataType="model:TodoItem">
                                <HorizontalStackLayout>
                                    <CheckBox IsChecked="{Binding IsCompleted}" />
                                    <Label Text="{Binding ItemName}" VerticalOptions="Center" />
                                    <Label Text="{Binding DueDate, StringFormat='Due: {0:MM/dd/yyyy}'}"
                                        VerticalOptions="Center" FontSize="12" TextColor="Gray" />
                                </HorizontalStackLayout>
                                <StackLayout.GestureRecognizers>
                                    <TapGestureRecognizer 
                                        NumberOfTapsRequired="1"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.NavigateToDetailCommand}"
                                        CommandParameter="{Binding .}" />
                                </StackLayout.GestureRecognizers>
                            </StackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>
